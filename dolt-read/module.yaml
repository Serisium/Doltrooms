product:
  type: kmp/lib
  platforms: [ jvm, android, iosArm64, iosSimulatorArm64, linuxX64 ]

description: |
  The doltrooms version-control READ machinery: the @DoltQuery reactive read support (annotations, generated-impl runtime, dynamic-table query builders). (SCAFFOLD — populated when the rev-4 contract implementation lands.)

dependencies:
  - ../dolt-core: exported

settings:
  android:
    namespace: dev.seri.doltrooms.dolt.read
    compileSdk: 36
    minSdk: 24
