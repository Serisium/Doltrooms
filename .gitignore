*.iml
.gradle
.idea
.DS_Store
build
captures
.externalNativeBuild
.cxx
local.properties
xcuserdata
.kotlin
#So we don't accidentally commit our private keys
*.gpg
#JVM fatal-error crash logs
hs_err_pid*.log
# The doltlite build plugin's fetchRemotesrv command output (all other
# plugin outputs live under the already-ignored build/ directory)
driver/native/build